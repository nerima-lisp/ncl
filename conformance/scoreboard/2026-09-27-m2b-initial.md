# M2b initial scoreboard

This is the first run against `origin/main` at `f4a917144a646b9c2c6e6a7c08fa08bf7a619ab2`.
The pinned source checkouts completed successfully:

- ansi-test: `ca06bd919661af162c67407c9d994e881870bdb3`
- cl-bench: `553fbcdf88d2ca4e340a0cdf02679055f3279c8f`

## Result

| suite | status | score |
| --- | --- | --- |
| ansi-test | failed during startup | passed/failed/unexecuted unavailable |
| cl-bench | failed during startup | completed benchmarks: 0 |

ansi-test stopped with `PackageNotFound("REGRESSION-TEST")` while loading
`doit.lsp`. cl-bench stopped with `constant requires a runtime table` before
producing benchmark samples. These are NCL execution failures, not source
acquisition failures.

The top-20 conformance failure ranking is not available from this run because
both suites stopped before emitting per-test results. The next implementation
priorities are therefore:

1. Load/register the `REGRESSION-TEST` package before ansi-test startup.
2. Fix the runtime-table requirement reached by cl-bench's constant loading.
3. Re-run ansi-test and capture its passed, failed, and unexecuted counts.
4. Re-run cl-bench and capture all completed samples and timings.

No additional ranking is inferred until per-test output exists.

## Reproduction

```sh
python3 scripts/conformance_scoreboard.py \
  --cache-dir /tmp/ncl-scoreboard-cache \
  --timeout 900 \
  --ansi-command 'sh -c "exec /tmp/ncl-scoreboard-target/release/ncl --load doit.lisp"' \
  --cl-bench-command 'sh -c "exec /tmp/ncl-scoreboard-target/release/ncl --eval (progn (load \\\"package.lisp\\\") (load \\\"cl-bench.lisp\\\") (funcall (read-from-string \\\"cl-bench:bench-run\\\")))"'
```

The source acquisition path retries the GitLab origin, falls back to the
matching GitHub mirror, and verifies the exact pinned commit after checkout.
