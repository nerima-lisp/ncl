# Compatibility

NCL is being rewritten. Compatibility claims for the new implementation will
be defined by the design contracts in `docs/src/design/` and the conformance
runner. The retired core ended at commit `d9bbb4ec`.
