[package]
name = "ncl-runtime"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
description = "The initial Lisp runtime for ncl"
license = "MIT"

[dependencies]
ncl-compiler = { path = "../compiler" }
ncl-syntax = { path = "../syntax" }
